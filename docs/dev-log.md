# יומן פיתוח (Dev Log)

יומן כרונולוגי של ההתקדמות בפרויקט. רישום קצר אחרי כל יום עבודה / שלב משמעותי: מה נעשה, מה נתקענו בו, ומה הצעד הבא.

הרשומה החדשה ביותר למעלה.

---

## 2026-08-29 — חשבון מנהל-על נמחק בלי שאיש ידע

- **התסמין:** לא הצלחתי להתחבר לאתר. **הסיבה:** החשבון
  `eyal.yinnon@gmail.com` נמחק — אפס רשומות ב-`auth.users` וב-`public.users`.
  ההודעה "המייל או הסיסמה שגויים" זהה למשתמש שאינו קיים ולסיסמה שגויה,
  ולכן היא לא הצביעה על כלום.
- **נשלל קודם:** הבנדל החי הצביע לאותו פרויקט, מפתח באורך 46, ואפילו
  hash הקובץ היה זהה לזה שנבדק ב-23.8 — כלומר לא הייתה פריסה מחדש
  והבעיה אינה באתר החדש.
- **הגילוי היה מקרי.** רק בגלל השוואה לתמונת מצב שנלקחה במקרה ב-21.8.
  `auth.audit_log_entries` של Supabase קיימת אך **ריקה**.
- **🔒 שני חורים נסגרו** ([מיגרציה
  014](../supabase/migrations/014_audit_and_last_admin.sql)):
  - **יומן `admin_audit`** על `users` ו-`authorities` — מי, מה, מתי,
    ואילו עמודות השתנו. **אינו ניתן לעריכה**: אין מדיניות כתיבה, והטריגר
    עוקף RLS. אומת ש-`DELETE`/`UPDATE` נוגעים ב-0 שורות ו-`INSERT` נדחה.
  - **הגנה על מנהל-העל האחרון** — חוסמת מחיקה, השהיה והורדת תפקיד.
    בטריגר ולא ב-Edge Function, כי המחיקה מגיעה בשרשור מ-`auth.users`.
- **`is_service_account`** — הסוכן הוא מנהל-על אבל אינו אדם. בלי לסמן
  אותו, אפשר היה למחוק את כל בני האדם והבדיקה הייתה עוברת.
- **מסך יומן** בתחתית ניהול הלקוחות
  ([AuditLog.tsx](../src/components/admin/AuditLog.tsx)).
- **פער ידוע:** פעולות שעוברות ב-Edge Function (יצירה, סיסמה, השהיה,
  מחיקה) רצות עם `service_role` ונרשמות בלי מבצע. השלמה דורשת פריסה
  מחדש של הפונקציה.
- **הצעד הבא:** לקמט ולדחוף — 013, 014, ותיעוד 008/009 ממתינים.

---

## 2026-08-23 — האחסון עבר לאיתי, והריפו התפצל

- **כתובת חדשה: https://moonlit-macaron-430f54.netlify.app** — רצה
  ב-Netlify של איתי. מה שנגמר אצלי היה **אשראי בניות**, לא תעבורה:
  האתר הוא 650KB סטטי וכל הנתונים מגיעים מ-Supabase.
- **התוכנית המקורית נכשלה.** רצינו שה-Netlify שלו ימשוך מהריפו שלי, אבל
  בורר הריפואים הציג רק את הריפואים שלו. הסיבה: **GitHub App מותקן לכל
  חשבון בנפרד**, וגישת קולבורייטור לריפו אישי של מישהו אחר לא נחשפת
  ל-Netlify באופן אמין.
- **הפתרון שנבחר — ריפו מראה** `EtaiNir/Project_Matzevet`, ו-`origin`
  עם **שני push URLs** כך שדחיפה אחת מגיעה לשניהם. נבחר על פני GitHub
  Action משכפל: אין טוקן לאחסן, ואם דחיפה נכשלת רואים את זה מיד.
- **הפריסה אומתה מול האתר החי**, לא רק "הדף עלה": תגובה `200`,
  הכותרות מ-`netlify.toml` נקלטו (`X-Robots-Tag: noindex`), ומפתח
  ה-anon בבנדל באורך **46 תווים** — שלושת המקומות שנפלנו בהם בעבר.
- **נתפס לפני הדחיפה:** הריפו של איתי נוצר `Public`. סריקה של כל
  הקבצים במעקב לא מצאה סודות או מיילים אמיתיים — רק פלייסהולדרים —
  אבל הסכימה, המיגרציות ותיאור פרצה שתוקנה כן שם. **טרם הוחזר
  ל-Private.**
- תועד ב-[decisions/009](decisions/009-hosting-split.md).
- **הצעד הבא:** להפוך את הריפו ל-Private, לעצור בניות באתר הישן,
  ובהמשך להעביר את הריפו לארגון — מה שמבטל את הדחיפה הכפולה לגמרי.

---

## 2026-08-20 — תיקוני פגישת 19.8 והגבלת ההעלאה

- **אייקון משפך לעמודה מסוננת.** סבא ביקש בפגישה בדיוק את הכפתור שהוסר
  יומיים קודם כי היו שני חצים זהים בכותרת. הפתרון היה בצורה ולא בקיום:
  חץ = מיון, **משפך** = סינון. אותו משפך גם פותח וגם מסמן — כשהעמודה
  מסוננת הוא נצבע והכותרת מקבלת רקע תכלת.
- **"נקה הכל" → "איפוס הכל".** קודם הוא ניקה *הכול* והשאיר מסך שאינו
  מצב פתיחה של שום תצורה. עכשיו מחזיר לנקודת האפס: סינון התצורה, בלי
  מיון ובלי זיהוי אחים.
- **חלון הסינון נגרר** מהכותרת; ההיסט יחסי לנקודת הפתיחה, כך שהוא
  ממשיך להיפתח ליד העמודה הנכונה גם אחרי הזזה.
- **אזהרה לפני פיווט** בריחוף מעל הכפתור — המלצה לסנן קודם, בלי לאיים
  על המשתמש בקריסה או להתחייב למספר מדויק.
- **🔒 העלאת מצב"ת הוגבלה למנהל רשות** ([מיגרציה
  012](../supabase/migrations/012_restrict_moe_upload.sql)). הממשק כבר
  הסתיר את הכפתור מצופה, **אבל ה-RLS התיר את הפעולה לכל מי שמשויך
  לרשות — כולל `viewer`**. משתמשת עם גישת צפייה בלבד יכלה לפנות ישירות
  ל-API ולגרום לטעינה מחדש של רשות שלמה. נאכף בשני מקומות: רישום
  ההעלאה והכתיבה לבאקט. אומת מול המשתמשים האמיתיים.
- **עריכת פרטי מועצה** ([מיגרציה
  011](../supabase/migrations/011_authority_update.sql)) — שם ניתן
  לעריכה והשבתה במקום מחיקה. **הקוד נעול בטריגר** ולא רק בממשק: הוא
  מרכיב את שם טבלת התלמידים, ושינוי שלו היה מנתק את הרשות מנתוניה בשקט.
- תועד ב-[decisions/008](decisions/008-public-pages-and-permissions.md)
  ובסיכום [הפגישה](meetings/2026-08-19-סבא-ואיתי.md).
- **הצעד הבא:** מעבר שנת לימודים 26→27 בסוכן — הסיכון הפתוח הגדול ביותר.

---

## 2026-08-19 — פנים ציבוריות למוצר

- **דף בית לפני הכניסה** במקום עמוד הקישורים שהיה אחריה. `/` `/about`
  `/contact` `/login` פתוחים לכולם; אחרי כניסה — מנהל-על ל-`/admin`,
  משתמש רגיל ל-`/students`. `Home.tsx` נמחק, אין מסך ביניים.
- **אין נתונים אמיתיים בדף הציבורי** — התצוגה המקדימה של הטבלה היא שלד
  עם כותרות אמיתיות ופסים אפורים. גם *להיראות* כאילו יש שם רשומות
  תלמידים זו טעות בדף שמועצות נכנסות אליו.
- **המספרים מתארים את המערכת ולא את העסק** (6 קבצים, ~160 שדות, 5
  מסכים) — כולם ניתנים לאימות בקוד.
- **סיבוב עיצוב שני** אחרי "נראה מאוד חובבני": אמוג'י הוחלף בסט
  [אייקוני SVG](../src/components/brand/Icons.tsx) בעובי קו אחיד, נוספו
  תצוגת טבלה מדומה ורקע שכבתי. כל הגרפיקה מוטבעת — אין קריאה החוצה.
- **הלוגו של סבא** הומר מ-JPEG לבן ל-PNG שקוף עם מעבר רך בקצוות (בלעדיו
  נשארת הילה לבנה), הוקטן ל-320px, 131KB → 63KB. משמש גם כ-favicon.
  `public/` נכנס לשימוש לראשונה.
- **דף פרטי קשר** נפרד עם הרקע המקצועי של סבא;
  [`config/site.ts`](../src/config/site.ts) מרכז את כל פרטי המותג במקום
  אחד, ואזור שתלוי בשדה ריק פשוט אינו מוצג.
- **הפרופיל לא נטען מחדש ברענון טוקן** — `onAuthStateChange` משודר גם
  ברענון (בערך כל שעה) והקוד טען פרופיל בכל פעם. נוסף `loadedFor`.
- **שדות תפקיד ומוסד למשתמש** ([מיגרציה
  010](../supabase/migrations/010_user_profile_fields.sql)) — תיאוריים
  בלבד, אינם משפיעים על הרשאות. הקריאה נופלת בחזרה לעמודות הבסיסיות אם
  המיגרציה טרם רצה, ולכן האתר עבד לפניה והשדות הופיעו אחריה בלי פריסה.
- **הצעד הבא:** תיקוני פגישת 19.8.

---

## 2026-08-18 — כל מה שאפשר לבנות בלי קבצים חדשים, מפגישת 13.8

- **בחירה מרובה בסינון** — הפער שסבא הגדיר "סופר-חשוב". לחיצה על כותרת
  עמודה (או רייט-קליק) פותחת את כל ערכיה לפי א"ב, עם צ'קבוקס וספירה
  לכל ערך, חיפוש, ובחר/בטל הכל. שלושה יישובים = שלוש לחיצות במקום
  שלושה סינונים נפרדים. תאים ריקים מקובצים ל`(ריק)` בסוף הרשימה.
- **הרשימה מחושבת אחרי שאר הסינונים ולפני זה של העמודה עצמה** — כמו
  באקסל. אחרת הורדת סימון הייתה מעלימה את שאר הערכים ואי אפשר היה
  להחזיר בחירה.
- **ייצוא אקסל מעוצב** — שמונה הדרישות של הערה 14, ובנוסף RTL וסינון
  אוטומטי. נכתב **כותב XLSX משלנו**: SheetJS החינמי אינו כותב עיצוב,
  ו-`npm install` נכשל ברשת הזו ב-`SELF_SIGNED_CERT_IN_CHAIN`. אומת
  בקריאה חוזרת עם openpyxl. תופעת לוואי: SheetJS יצא מהבנדל, וקובץ של
  8,000 שורות ירד מ-~1MB ל-0.22MB.
- **דף פיבוטים** — שישה דוחות מוגדרים מראש (מוסד×שכבה, מוסד×כיתה,
  מוסד×יישוב, יישוב×שכבה, תלמידי חוץ, חינוך מיוחד) ובוררי צירים
  חופשיים. **הסינון נוסע עם הניווט** מהטבלה, בדיוק כזרימה שסבא תיאר.
- **שני השדות המחושבים התמלאו** — `סטטוס חינוך מיוחד` (משבצת) לפי הכלל
  שסבא הדגים חי, ו`סטטוס תלמיד ברשות` לפי `moe_code`. שניהם נגזרים
  **בתצוגה** ולא ב-pipeline: ההגדרה עוד מתלטשת, ושינוי בצד הלקוח הוא
  פריסה במקום ריצה מחדש על השרת.
- **מסך מגמות** (שכבות י׳–י״ב) ו**מסך חינוך מיוחד שנפתח כבר מסונן**.
- **טעינה של 20–30 שניות** — ספירה מראש ובקשות מקבילות במקום דפדוף
  סדרתי, ומטמון לפי רשות. חזרה למסך מיידית; כפתור "רענון" מנקה.
- **עמוד בית** כנקודת פתיחה קבועה — סבא נכנס וקיבל את הרשות האחרונה
  שהיה בה במקום מסך פתיחה. בנוסף: "חזרה לניהול" בלי לצאת מהמערכת,
  עין לצפייה בסיסמה, וזהות מלאה בכותרת.
- **מנהל רשות יכול לעדכן מצב"ת בעצמו** — `/upload/:code`. ה-RLS כבר
  התיר זאת; חסר היה רק מסך.
- 40 בדיקות לוגיקה + אימות קובץ האקסל. `tsc`, `build` ו-`lint` נקיים.
- **הצעד הבא:** אישור [מיגרציה 010](../supabase/migrations/010_user_profile_fields.sql)
  (תפקיד ומוסד למשתמש), בדיקה בעין על נתוני אמת, וקבלת הקבצים שחוסמים
  את הקבוצה השנייה — גנים, לידה עד 3, ורשימת תלמידי שילוב.

---

## 2026-08-18 — אימות החבילה, ותקלת העלאה שהתבררה כתוסף VPN

- **הסוכן עלה אצל סבא ועובד** — שתי העלאות מהאתר לרשות 120 הושלמו.
- **אימות מלא של החבילה**: התקנה מאפס מה-ZIP לתיקייה ריקה, כולל venv,
  חמש הספריות, `check_setup`, 25 הבדיקות, וההפעלה הראשונה מול תיקייה
  שכבר יש בה 12 קבצים — נרשם מצב פתיחה, לא עובד כלום, והעדכון שהגיע
  אחריו עובד לבדו ב-86 שניות.
- **תוקנו ארבעה פערים בין התיעוד למציאות**: שם המשימה ב-`agent/README`
  (ה-PDF תוקן בסבב הקודם, ה-README לא), בדיקות שתועדו ולא היו קיימות
  בדיסק, הנחיה מיושנת ב-`DataTab` להריץ `update_from_moe.py` ידנית,
  וספירת מיגרציות שגויה ב-`docs/README`.
- **`Unexpected token '<'` בהעלאה** — הצד השרתי נשלל לגמרי (ששת הקבצים
  עולים ב-200, RLS עוברת, 60MB מגיעים ל-Supabase), והאשם הוא **תוסף VPN
  בכרום** שמחזיר דף HTML במקום להעביר בקשה של 3.9MB. נוסף דיווח שאומר
  איזה שלב נכשל; לא נבנה מעקף.
  פרטים: [decisions/006](decisions/006-upload-failures-and-vpn.md).
- **הצעד הבא:** התקנה על השרת של סבא מהחבילה המאומתת.

---

## 2026-08-17 — חיבור לתיקייה קיימת, ותיקון README שנופח

- **הסוכן היה מעדכן את כל המועצות ברגע שמחברים אותו לתיקייה של סבא.**
  התיקייה שם מתעדכנת כל חודש כבר שבע שנים, ובסבב הראשון כל מועצה נראתה
  "השתנתה". מעכשיו סבב ראשון מול תיקייה חדשה רק **רושם מצב פתיחה** ויוצא;
  מכאן ואילך מעובד רק מה שמתעדכן. `--process-existing` לטעינה מכוונת.
  פרטים: [decisions/005](decisions/005-watch-folder-baseline.md).
- **טביעת האצבע ויתרה על `mtime`** — סנכרון שמעתיק את התיקייה כולה מדי
  חודש היה נראה כמו עדכון של כל המועצות. שם הקובץ נושא את חותמת משרד
  החינוך ולכן ייחודי ממילא.
- נבדק מול 168 קבצים / 14 עדכונים / 2 מועצות: החיבור לא עיבד כלום, העדכון
  שהגיע אחריו עובד לבדו.
- **נוספה תיקיית [agent/tests/](../agent/tests/)** — 25 בדיקות סינתטיות
  שאינן נוגעות במסד: מצב פתיחה, בחירת קבצים ושיוך למועצה, ומחיקת גיבויים.
  עד עכשיו כל הבדיקות נכתבו בתיקייה זמנית ונמחקו בסוף הסשן, כך שהתיעוד
  הפנה לקבצים שלא היו קיימים.
- **מנגנון הגיבוי נדרש בפועל בפעם הראשונה** — פיקסצ'ר פגום בבדיקה גרם
  לטעינת נתונים זרים ל-`students_120`, והטבלה שוחזרה מהגיבוי שנלקח רגע
  קודם (4,881 שורות, 0 ת"ז משותפות).
- **`agent/README.md` היה 10MB, 197,703 שורות.** סקריפט התיעוד של שני
  הסבבים הקודמים הוסיף פרק במקום להחליף אותו, וכל הרצה שרשרה עוד עותק —
  5,985 עותקים של "שחזור מגיבוי". שוחזר מהגרסה הנקייה והעדכונים הוחלו
  מחדש בהחלפה. שתי הגרסאות המנופחות כבר נדחפו וקיימות בהיסטוריה.
- **התיעוד נתן פקודות שנכשלות** — `-TaskName 'סוכן מצבת תלמידים'` בארבעה
  מקומות ב-PDF ובשלושה ב-`agent/README.md`, בעוד המשימה נרשמת כ-`MatzevetAgent`
  מאז תיקון ההתקנה. בסבב הראשון תוקן ה-PDF בלבד; ה-README נמצא אחריו.
- **`DataTab` עדיין הנחה להריץ `update_from_moe.py` ידנית** אחרי העלאה —
  טקסט מלפני הסוכן. הוחלף בהפניה להיסטוריית העדכונים.
- **הצעד הבא:** התקנה על השרת של סבא מהחבילה הבדוקה.

---

## 2026-08-16 — סריקת תיקייה מקומית, גיבוי לאקסל ותיקוני התקנה

- **ההתקנה אצל סבא נכשלה** ושלושת השורשים אותרו — כולם קידוד וגרסאות:
  קובצי `.bat` עם עברית ב-UTF-8 שוברים את הפרסר של `cmd.exe` (ולכן
  הסביבה הווירטואלית לא הופעלה והכול גלש לפייתון הגלובלי); `.ps1` ללא
  BOM נקרא כ-ANSI ב-PowerShell 5.1; ו-`pandas==2.2.3` המקובע אין לו wheel
  לפייתון 3.14. תוקן: `.bat` ב-ASCII עם קריאה ישירה ל-venv, `.ps1` עם BOM,
  ו-`requirements` עם `>=` — **נבדק שיש wheels גם ל-3.11 וגם ל-3.14**.
- **הותקן מאפס בתיקייה נקייה** ואומת מקצה לקצה, כולל רישום כמשימה 24/7.
- **סריקת תיקייה מקומית** — הסוכן סורק כל דקה תיקייה שאליה מגיעים קובצי
  משרד החינוך, מקבץ לפי קוד הרשות ומעבד כל מועצה שהשתנתה.
  **נבדק על 12 קבצים של שתי מועצות יחד**: 8,244 למטה מנשה, 4,881 לכפר,
  **אפס ת.ז. משותפות**.
- **הגיבוי עבר לאקסל** במקום `csv.gz` — נפתח בלחיצה כפולה, גיליון RTL,
  ות.ז. נשמרות כטקסט כדי שאקסל לא ימחק אפסים מובילים.
- **מיגרציה 009** — `moe_code`: קוד הרשות אצלנו אינו בהכרח זה של משרד
  החינוך (כפר = 120 אצלנו, 5108 שם). נלמד בעדכון הראשון, ומכאן חוסם
  טעינת קבצים של מועצה אחת לטבלה של אחרת.
- נוספו `check_setup.py` (בדיקת חיבור אמיתית בסוף ההתקנה),
  `uninstall.ps1`, הסלמת הרשאות אוטומטית ו-`install-service.log`.
- **הצעד הבא:** התקנת הסוכן אצל סבא מהחבילה המתוקנת.

---

## 2026-08-13 — ממשק מנהל, הרשאות מדורגות וסוכן עיבוד

- **ממשק מנהל** — כרטיס לקוח לכל מועצה, ולכל אחת שלושה מסכים: התנהלות
  עסקית (מועדי תשלום, חידוש הסכם, מסמכים), ניהול הרשאות, ונתוני המועצה
  עם העלאת 6 קבצי המצב"ת. גישה לפי `role='super_admin'`, לא לפי סיסמה קסומה.
- **הרשאות מדורגות** — מועצתי / יישובי / בית-ספרי, נאכפות ב-RLS.
  נבדק: יישובי 1,016 · בית-ספרי 1,282 · מועצתי 8,244 — התאמה מדויקת.
- **Edge Function `admin-users`** — יצירת משתמש, קביעת סיסמה, השהיה ומחיקה.
  מחזיקה את `service_role` בצד השרת ומאמתת בעצמה שהקורא מנהל-על.
- **השהיית משתמש** בשני רבדים: ban ב-Auth + דגל `is_suspended` שמנטרל את
  פונקציות ההרשאה — אחרת טוקן קיים היה ממשיך לעבוד עוד כשעה.
- **הוספת מועצה מהממשק** — [`create_authority`](../supabase/migrations/006_create_authority.sql)
  יוצרת גם את טבלת התלמידים, ה-RLS והאינדקסים.
- **סוכן עיבוד** ([agent/](../agent/README.md)) — רץ 24/7 על מחשב נפרד,
  בודק כל 5 שניות, מוריד את הקבצים שהועלו, מריץ את ה-pipeline, יוצר טבלה
  לרשות חדשה, **מגבה את הקיימת לפני דריסה**, וטוען. מזדהה עם חשבון ייעודי
  ולא עם `service_role`.
- **נבדק על נתוני אמת:** הסוכן עיבד העלאה של רשות גלבוע (800000) שלא הייתה
  בשום מיגרציה — יצר לה טבלה וטען 7,833 תלמידים, בלי SQL ידני.
- **תוקן באג בשימוש:** כרטיסי לקוח הראו 0 תלמידים לרשויות שלא שויכו למנהל.
  [מיגרציה 008](../supabase/migrations/008_super_admin_sees_all.sql) — מנהל-על
  רואה את כל הרשויות. הבידוד למשתמש רגיל נבדק ונשמר.
- **חבילת התקנה** — `matzevet-agent.zip` (5.8MB) ומדריך PDF בן 14 עמודים,
  ב-`agent/dist/`. שניהם מחוץ לגיט.
- החלטות תועדו ב-[decisions/004](decisions/004-admin-interface-and-agent.md).
- **הצעד הבא:** התקנת הסוכן אצל סבא, ודומיין משלנו.

---

## 2026-08-03 — איחוד הצינור, השלמת המילון, ועלייה לאוויר

- **שני הצינורות אוחדו לאחד.** נבנה [update_from_moe.py](../scripts/update_from_moe.py):
  pipeline של איתי → איתור הקובץ הראשי → טעינה ל-Supabase, בפקודה אחת.
  ה-StudentsImporter (C#) אינו בשימוש בזרימה החדשה אך לא נמחק.
- **התגלה פער של 325 תלמידים** בין הגדרת "תלמיד פעיל" של האקסס לזו של האתר —
  317 מהם במצב "עזב". ההחלטה: **לטעון את כולם ולסנן ב-UI**, לא בעיבוד.
- **המילון הושלם:** 111 → 162 רשומות. המיפוי עלה **110 → 158 עמודות**.
  נוספו טלפוני הורים, מיילים וכתובות מרשם — הפער שחסם את תצורת "פרטי קשר".
  [מיגרציה 003](../supabase/migrations/003_contact_and_status_fields.sql), 48 עמודות.
- **שלושה חסמים במודול 7 תוקנו:** תבנית קבועה של 157 עמודות, סינון שמחק
  269 תלמידים (מהם 14 "משובץ") בשקט, והתנגשות שמות בעמודת הסטטוס.
- **האתר עלה ל-Netlify** — https://astounding-pudding-d07e8f.netlify.app
  מחובר לגיטהאב (ריפו פרטי), פריסה אוטומטית בכל push.
- **ה-pipeline הוכן למעבר לשרת** — נתיבים יחסיים, `os.path.join` (19 מקומות),
  משתני סביבה. ראה [server-migration.md](server-migration.md).
- **אבחון כניסה:** מפתח ה-anon הודבק ל-Netlify קטוע ב-5 תווים והשגיאה נראתה
  כמו סיסמה שגויה. נבנה [diagnostics.ts](../src/lib/diagnostics.ts) + תיבת
  "פרטים טכניים" במסך הכניסה.
- **עיצוב:** בורר שדות עם חיפוש (160+ שדות), כותרות עמודות בתכלת, שורות
  לסירוגין, וליטוש כללי.
- החלטות תועדו ב-[decisions/003](decisions/003-unified-pipeline-and-cloud.md).
- **הצעד הבא:** סיבוב `service_role` key, דומיין משלנו, והכרעת סבא על
  325 התלמידים שעזבו.

---

## 2026-07-01 — הקמת Supabase, טעינת נתונים והרצה חיה

- **הוקם פרויקט Supabase אמיתי** (`agcoeqshvkyopjjdvril`) והורצו המיגרציות דרך
  [run_migrations.py](../scripts/run_migrations.py) (חיבור session pooler, IPv4).
- **נטענו 7,905 תלמידים אמיתיים** של מטה מנשה (1400000) דרך [load_main.py](../scripts/load_main.py).
- **נוצר משתמש admin** (`eyal.yinnon@gmail.com`) משויך לרשות, דרך [create_user.py](../scripts/create_user.py).
- נבדקה כל השרשרת: כניסה + RLS + שאילתה מחזירים נתוני אמת.
- **תוקן באג טעינה:** `df.to_numpy()` המיר ריקים ל-`float nan` שנכתבו כ-`'NaN'` בטקסט — תוקן
  (nan→None). כן חושב `כיתה משולב` (ט5→ט-5) ותוקן מיפוי שמות הורים דו-משמעי.
- **עיקרון שאושר:** תא ריק במקור נשאר ריק באתר — בלי ערכים מזויפים.
- **תיעוד:** נוצרו [architecture.md](architecture.md), [database.md](database.md),
  [data-loading.md](data-loading.md), [features.md](features.md), [setup-and-run.md](setup-and-run.md),
  ו-[decisions/002](decisions/002-supabase-deployment.md).
- **הצעד הבא:** פריסה ל-Netlify, מסך ניהול משתמשים, וסיבוב מפתחות שנחשפו.

---

## 2026-07-01 — מימוש שלב א' (MVP)

- נבנתה אפליקציית React + TypeScript + Vite + Tailwind (RTL מלא) ב-`student-dashboard/`.
- **קונפיג שדות:** [src/config/fields.ts](../src/config/fields.ts) — ~117 שדות עם תוויות עברית,
  סוגים וקבוצות, נגזר מ-`columns_name_dictionary.xlsx`. + 3 שדות מחושבים.
- **4 תצורות** ב-[src/config/presets.ts](../src/config/presets.ts): תלמידים, פרטי קשר, מוסדות, חינוך מיוחד.
- **רכיבים:** StudentTable (וירטואליזציה + מיון + דפדוף שדות), FilterBar (כל האופרטורים, AND),
  FieldPicker, StudentCard (כרטיס תואם-תצורה, ניווט הבא/קודם, זיהוי אחים).
- **מנועים:** filters.ts (סינון), table.ts (מיון + דפדוף שדות), exportExcel.ts (ייצוא), AuthContext.
- **DB:** מיגרציות Supabase — 001 (users/authorities/RLS + has_authority), 002 (students_1400000).
- **טעינת נתונים:** [scripts/load_data.py](../scripts/load_data.py).
- החלטות טכניות תועדו ב-[decisions/001-tech-choices.md](decisions/001-tech-choices.md).
- **הצעד הבא:** הקמת פרויקט Supabase אמיתי, הרצת מיגרציות, טעינת נתוני 1400000, ובדיקת end-to-end.

---

## 2026-06-30 — הקמת מבנה הפרויקט

- הוקם מבנה התיקיות `student-dashboard/` עם תיקיית `docs/` לאובסידיאן.
- מסמך האפיון המלא הועתק ל-[project-spec.md](project-spec.md).
- נוצרו תיקיות `decisions/` ו-`meetings/` עם תבניות.
- נוצרו `CLAUDE.md` (הוראות ל-Claude Code) ו-`package.json` בשורש.
- **הצעד הבא:** התחלת שלב א' — הקמת Supabase, מערכת הרשאות, וטבלה ראשית.
