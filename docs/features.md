# פיצ'רים — מה מומש בשלב א'

מיפוי בין דרישות האפיון (§7 שלב א') לבין המימוש בקוד.

| # | פיצ'ר | קובץ עיקרי | סטטוס |
|---|--------|-----------|--------|
| 1 | מסד נתונים בענן, טבלה לכל רשות | `supabase/migrations/` | ✅ |
| 2 | הרשאות: כניסה, שיוך לרשות | `context/AuthContext.tsx`, `pages/Login.tsx` | ✅ |
| 3 | טבלה ראשית — כל התלמידים + שדות ליבה | `components/StudentTable.tsx` | ✅ |
| 4 | 5 תצורות (presets) | `config/presets.ts` | ✅ |
| 5 | בורר שדות (~160 שדות) | `components/FieldPicker.tsx` | ✅ |
| 6 | דפדוף שדות אופקי | `lib/table.ts` + `pages/Dashboard.tsx` | ✅ |
| 7 | סינון מתקדם (כולל "מכיל") | `components/FilterBar.tsx` + `lib/filters.ts` | ✅ |
| 8 | מיון עולה/יורד | `lib/table.ts` + `StudentTable.tsx` | ✅ |
| 9 | כרטיס תלמיד + ניווט הבא/קודם | `components/StudentCard.tsx` | ✅ |
| 10 | זיהוי אחים | `StudentCard.tsx` + `Dashboard.tsx` | ✅ |
| 11 | ייצוא לאקסל | `lib/exportExcel.ts` | ✅ |

## פירוט

### 5 התצורות (`config/presets.ts`)
טבלה מרכזית **אחת**; טאבים מחליפים בין קבוצות שדות ברירת מחדל:
1. **תלמידים** — מידע פדגוגי/אישי.
2. **פרטי קשר והורים** — כולל זיהוי אחים.
3. **מוסדות חינוך** — מיקוד במוסד.
4. **חינוך מיוחד** — משבצת/זכאות. נפתחת כבר מסוננת.
5. **מגמות** — מסלולים ומגמות בתיכון. נפתחת מסוננת לשכבות י׳–י״ב.

ארבע הראשונות הן אלה שבאפיון (§5); **מגמות נוספה בסיבוב של פגישת 13.8**
לבקשת סבא, ולכן `project-spec.md` עדיין מונה ארבע.

### שדות (`config/fields.ts`)
מקור אמת אחד לכל השדות: מפתח DB (אנגלית) ↔ תווית (עברית) ↔ סוג (טקסט/מספר/תאריך) ↔ קבוצה
(לארגון הכרטיס). נגזר מ-`columns_name_dictionary.xlsx`.

### סינון (`lib/filters.ts`)
אופרטורים: שווה, שונה, **מכיל** ⭐, אינו מכיל, מתחיל ב, מסתיים ב, גדול/קטן מ, בין...ל,
ריק, לא ריק, אחד מתוך. שילוב מספר תנאים ב-AND. הסינון קובע גם את הייצוא וגם את ניווט הכרטיס.

### זיהוי אחים
לחיצה על ת.ז. הורה בכרטיס מציגה את כל התלמידים שאחד מהוריהם הוא ת.ז. זו (התאמת OR על
`GOREM_KESHER_1_ID` / `GOREM_KESHER_2_ID`). שימוש קריטי במצבי חירום.

### טבלה עם וירטואליזציה (`StudentTable.tsx`)
מבנה flex אחיד לכותרת ולשורות, שורות מווירטואלות (`@tanstack/react-virtual`) לתמיכה
ברשות גדולה (~50k תלמידים). דפדוף שדות אופקי כשהעמודות חורגות מרוחב המסך.

### מצב רישום וסינון ברירת מחדל
הטבלה מכילה את כל התלמידים (8,244), כולל מי שעזב. סינון ברירת המחדל מצמצם
ל`מצב רישום = משובץ` (7,919), **מוצג בסרגל הסינון וניתן להסרה בלחיצה**.

### בורר שדה עם חיפוש (`FieldCombobox.tsx`)
בסרגל הסינון, בחירת השדה משלבת הקלדה לחיפוש ורשימה נפתחת. החיפוש חל על
התווית בעברית ועל שם העמודה, עם ניווט במקלדת. נדרש בגלל 160+ השדות.

### אבחון תקלות כניסה (`lib/diagnostics.ts`)
תיבת "פרטים טכניים" במסך הכניסה מבחינה בין תקלת הגדרה (מפתח API פגום)
לבין פרטים שגויים, ומדווחת את אורך המפתח שהגיע בפועל.

---

# הסיבוב של פגישת 13.8 (2026-08-18)

| # | פיצ'ר | קובץ עיקרי | סטטוס |
|---|--------|-----------|--------|
| 1 | בחירה מרובה בסינון, מכותרת העמודה או ברייט-קליק | `components/ColumnFilterMenu.tsx` + `lib/columnValues.ts` | ✅ |
| 2 | בחר הכל / בטל הכל / חזרה לברירת מחדל בבורר השדות | `components/FieldPicker.tsx` | ✅ |
| 3 | מסך מתמחה — מגמות (י׳–י״ב) | `config/presets.ts` | ✅ |
| 4 | חינוך מיוחד — סימון משבצת ומסך שנפתח מסונן | `lib/computed.ts` | ✅ |
| 5 | סטטוס תלמיד ברשות (גר/לומד/תלמיד חוץ) | `lib/computed.ts` | ✅ |
| 6 | שם רשות המוסד ושם רשות התלמיד בכל התצורות | `config/presets.ts` | ✅ |
| 7 | פיבוטים — 6 מוגדרים מראש + צירים חופשיים | `pages/Pivot.tsx` + `lib/pivot.ts` | ✅ |
| 8 | ייצוא אקסל מעוצב (8 דרישות + RTL + סינון) | `lib/xlsx.ts` + `lib/exportExcel.ts` | ✅ |
| 9 | עמוד בית כנקודת פתיחה | ~~`pages/Home.tsx`~~ → `pages/Landing.tsx` | ✅ הוחלף — ראה למטה |
| 10 | חזרה לניהול בלי לצאת מהמערכת | `pages/Dashboard.tsx` | ✅ |
| 11 | עין לצפייה בסיסמה | `pages/Login.tsx` | ✅ |
| 12 | זהות מלאה בכותרת (תפקיד · מוסד · הרשאה) | `context/AuthContext.tsx` + `admin/PermissionsTab.tsx` | ✅ |
| 15 | סמל מסלול — משוחזר ממיפוי הפוך, בלי שינוי ב-pipeline | `lib/computed.ts` | ✅ |
| 13 | עדכון מצב"ת בידי מנהל רשות | `pages/UpdateData.tsx` | ✅ |
| 14 | טעינה מהירה — מטמון ובקשות מקבילות | `lib/students.ts` | ✅ |

פרטים והנמקות: [decisions/007](decisions/007-client-side-features.md).

---

# הדפים הציבוריים (2026-08-19)

ארבעה מסלולים פתוחים למי שאינו מחובר, תחת
[`PublicLayout`](../src/components/PublicLayout.tsx) — סרגל ניווט וכותרת
תחתונה עם הלוגו ופרטי הקשר.

| מסלול | קובץ | תוכן |
|--------|------|------|
| `/` | `pages/Landing.tsx` | כותרת ראשית, תצוגת טבלה מדומה, נתוני מערכת, שלושה שלבים, שישה אריחי יכולות, אזור פרטיות |
| `/about` | `pages/About.tsx` | הסבר על המוצר ועל תהליך העבודה |
| `/contact` | `pages/Contact.tsx` | מאיר יפה — רקע מקצועי, הסמכות, ניסיון, ודרכי התקשרות |
| `/login` | `pages/Login.tsx` | כניסה, עם קישורים חזרה לדף הבית ולאודות |

**ניתוב אחרי כניסה** ([`App.tsx`](../src/App.tsx)): מנהל-על → `/admin`,
משתמש רגיל → `/students`. אין מסך ביניים.

**מותג במקום אחד** — [`config/site.ts`](../src/config/site.ts) מרכז שם,
תגית, ופרטי הבעלים. [`brand/Logo.tsx`](../src/components/brand/Logo.tsx)
מייצא גם את צבעי הלוגו (`LOGO_GREEN` `LOGO_BLUE` `LOGO_INDIGO`), ו-
[`brand/Icons.tsx`](../src/components/brand/Icons.tsx) מספק תשעה אייקוני
SVG בקווי מתאר. **כל הגרפיקה מוטבעת** — אין קריאה לשרת חיצוני מדף
שרשות נכנסת אליו.

---

# תיקוני פגישת 19.8 (2026-08-20)

| # | תיקון | קובץ | סטטוס |
|---|--------|------|--------|
| 1 | "איפוס הכל" מחזיר לברירת המחדל במקום לרוקן | `components/FilterBar.tsx` + `pages/Dashboard.tsx` | ✅ |
| 2 | אייקון **משפך** בכותרת — פותח סינון וגם מסמן שהעמודה מסוננת | `components/StudentTable.tsx` | ✅ |
| 3 | כותרת עמודה מסוננת נצבעת בתכלת | `components/StudentTable.tsx` | ✅ |
| 4 | חלון הסינון נגרר מהכותרת שלו | `components/ColumnFilterMenu.tsx` | ✅ |
| 5 | אזהרה בריחוף מעל כפתור הפיווט — מומלץ לסנן קודם | `pages/Dashboard.tsx` | ✅ |
| 6 | עריכת שם מועצה + השבתה | `admin/BusinessTab.tsx` + מיגרציה 011 | ✅ |
| 7 | 🔒 העלאת מצב"ת — מנהל רשות בלבד, נאכף ב-RLS | מיגרציה 012 | ✅ |

**מוסכמת האייקונים בכותרת:** חץ = מיון · משפך = סינון. שני חצים זהים
שעשו דברים שונים גרמו לשלוש איטרציות — ראה
[decisions/008](decisions/008-public-pages-and-permissions.md).

**מחוות בטבלה:** לחיצה שמאלית על תא → כרטיס תלמיד · לחיצה **ימנית** על
תא → תפריט (`CellActionMenu.tsx`) עם כרטיס תלמיד וסינון לפי הערך ·
לחיצה על כותרת → מיון · לחיצה על המשפך → בחירה מרובה.

---

## מה עדיין לא מומש
- **מעבר שנת לימודים 26→27** — ביוני–יולי מגיעים 12 קבצים, שישה לכל
  שנה, והסוכן עלול לדרוס שנה בשנייה. **הסיכון הפתוח הגדול ביותר.**
- **חינוך מיוחד — שילוב**: ממתין לרשימת ת"ז מהשירות הפסיכולוגי.
- **הצגת סוג הליקוי** — סבא ביקש הגדרה **פר-מועצה**, לא גלובלית.
- **קליטת דרג ב'** (גנים, לידה עד 3, קידום נוער, חינוך ביתי) —
  ממתינה לקבצים ולאישור [התבנית](tier-b-template.md).
- **נתונים תוספתיים לעריכה** — [אפיון](extra-fields-design.md) מוכן;
  סבא תיזמן לאחר היציאה לשיווק. המערכת נשארת **פסיבית** בשלב זה.
- **גישה להיסטוריית הגיבויים מהממשק** — הסוכן שומר גיבוי לפני כל דריסה,
  אך אין דרך לראות אותו מהאתר.
- **התאמה לניידים** — ממתינה להכרעה אילו שדות מוצגים בכרטיס הנייד.
- **מיתוג בארבעת מסכי הנתונים** — החומרים התקבלו ומוטמעים בדפים
  הציבוריים; מסכי הנתונים עדיין לא.
- **דומיין משלנו** במקום כתובת `netlify.app` אקראית.
- **אסמכתת אבטחה ללקוח** — *"תוכיח לי שהנתונים מוגנים כהלכה."*

---

# ממשק המנהל (2026-08-13)

נגיש למשתמש עם `role='super_admin'` בלבד, שמנותב אליו אוטומטית בכניסה.

| מסך | קובץ | מה יש בו |
|------|------|----------|
| רשימת לקוחות | `pages/Admin.tsx` | כרטיס לכל מועצה: מספר תלמידים, מועדי תשלום וחידוש צבועים לפי דחיפות. כפתור הוספת מועצה |
| התנהלות עסקית | `components/admin/BusinessTab.tsx` | **פרטי המועצה** (שם לעריכה, קוד נעול, פעילה), פרטי קשר, מועדים כספיים, הערות, ומסמכים כאריחים |
| ניהול הרשאות | `components/admin/PermissionsTab.tsx` | תפקיד, היקף, תפקיד ומוסד (תיאורי), סיסמה, השהיה, מחיקה, ויצירת משתמש |
| נתוני המועצה | `components/admin/DataTab.tsx` | קישור לטבלה + העלאת 6 קבצי מצב"ת + היסטוריית עדכונים. **ההעלאה למנהל רשות בלבד** |

## היקף הצפייה

| רמה | רואה |
|------|------|
| מועצתי | כל תלמידי הרשות |
| יישובי | יישובים נבחרים בלבד |
| בית-ספרי | מוסדות נבחרים בלבד |

הבחירה נעשית מרשימה שנטענת מהנתונים עצמם (269 יישובים / 214 מוסדות ברשות
1400000), עם חיפוש. **האכיפה ב-RLS ולא בתצוגה.**

## סוכן העיבוד

רץ 24/7 על מחשב נפרד ומעבד את הקבצים שהועלו. פרטים:
[agent/README.md](../agent/README.md) · [decisions/004](decisions/004-admin-interface-and-agent.md).
