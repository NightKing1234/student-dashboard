# CLAUDE.md

## מה הפרויקט הזה

ממשק אינטרנטי לניהול נתוני תלמידים ממשרד החינוך, מיועד למועצות אזוריות בישראל. מחליף מערכת Access קיימת.

**מסמך האפיון המלא נמצא ב-`docs/project-spec.md` — תקרא אותו לפני שאתה מתחיל לעבוד על כל משימה.**

## Tech Stack

- **Frontend:** React + Tailwind CSS
- **Database:** Supabase (PostgreSQL)
- **Auth:** Supabase Auth (מייל + סיסמה)
- **Hosting:** Netlify — https://astounding-pudding-d07e8f.netlify.app
  (ריפו פרטי `NightKing1234/student-dashboard`, פריסה אוטומטית בכל push)
- **Export:** SheetJS (xlsx) לייצוא אקסל מצד הלקוח
- **Language:** TypeScript

## כללים קריטיים

- **RTL מלא** — כל הממשק בעברית, direction: rtl, יישור לימין
- **כתוב עברית ישירות בקוד** — לא Unicode escapes, לא משתנים באנגלית לטקסטים בעברית
- **רשות = עולם** — כל רשות מקומית רואה רק את הנתונים שלה. טבלה נפרדת לכל רשות ב-Supabase
- **הכל ואז מצמצמים** — הטבלה נפתחת עם כל התלמידים, המשתמש מסנן משם
- **ייצוא לאקסל = הדוח היחיד** — אין מחולל דוחות, המשתמש בונה תמהיל שדות ומייצא
- **תא ריק במקור נשאר ריק באתר** — לא ממציאים ערכים, לא ממלאים ברירות מחדל
- **לא מסננים תלמידים בעיבוד** — טוענים הכל ומסמנים; הסינון קורה בתצוגה.
  סבא: *"עדיף להשאיר תלמיד במערכת מאשר למחוק אותו"*

## 🔒 פרטיות — הכלל שגובר על הכל

הנתונים כוללים **תעודות זהות, כתובות וטלפונים של אלפי קטינים**.

- **קבצי מצב"ת לעולם לא עולים לגיט** ולא ל-GitHub Actions. הריפו כולל את
  `student-dashboard/` בלבד; `Itay_Modules/` נשאר מקומי.
- `.env` ו-`.env.db` ב-`.gitignore` — לעולם לא לכתוב סוד בקוד או בתיעוד.
- באתר: כותרת `X-Robots-Tag: noindex` ב-`netlify.toml`.
- רק ה-**anon key** נכנס לפרונטאנד. `service_role` משמש סקריפטים בלבד.

## מלכודות ידועות (נתקלנו בהן — אל תיפול שוב)

1. **המילון הוא צוואר הבקבוק.** `load_main.py` ממפה דרך
   `columns_name_dictionary.xlsx` ו**זורק בשקט** כל עמודה שאינה בו. אם שדה
   לא מופיע באתר — לבדוק שם *לפני* שמחפשים באג בקוד.
2. **מודול 7 בונה את הפלט מתבנית קבועה** (`School_Headers_Template.xlsx`).
   עמודה חדשה חייבת להתווסף ל-`EXTRA_OUTPUT_COLUMNS`, אחרת היא נופלת.
3. **`CODE_STATUS_RISHUM_TA` בטבלה אינו סטטוס הרישום** — המילון ממפה אליו
   את `תקינות שיבוץ`. סטטוס הרישום האמיתי הוא `MATZAV_RISHUM_CODE/TEUR`.
4. **הפרונטאנד לא מוודא שמשתני הסביבה תקינים בזמן בנייה.** מפתח קטוע גורם
   לשגיאה שנראית כמו סיסמה שגויה. ראה `lib/diagnostics.ts`.
5. **ה-pipeline לא יכול לרוץ ב-Netlify** — pandas, 30 שניות, קבצי אקסל.
   הוא רץ בסוכן על מחשב נפרד ([agent/](agent/README.md)).
6. **`copy_expert` משאיר טרנזקציה פתוחה.** היא מחזיקה נעילה על הטבלה,
   וה-`TRUNCATE` של הטעינה ייתקע עד פקיעת זמן. חובה `conn.commit()` אחריו.
7. **`supabase-js` מסתיר את גוף השגיאה** של Edge Function ומחזיר
   `"non-2xx status code"` גנרי. ההודעה האמיתית ב-`error.context` (Response).
8. **Edge Functions:** `jsr:` אינו נתמך (יש להשתמש ב-`esm.sh`), ופריסה
   חייבת `entrypoint_path` — אחרת הפונקציה מדווחת `ACTIVE` ונופלת ב-`BOOT_ERROR`.
9. **סקריפט שאורז קבצים חייב לחסום את תיקיית היעד של עצמו.** אחרת הוא
   אורז את הפלט שלו שוב ושוב — קרה, וה-ZIP הגיע ל-23GB.
10. **חסימה לפי סיומת אינה מספיקה.** גיבויים הם `.csv.gz` — חסימת `.csv`
    לא תפסה אותם, והם כמעט נכנסו לחבילה שנשלחת החוצה.

## הרשאות — שלוש שכבות

| שכבה | קובע |
|-------|------|
| `role` | מה מותר לעשות (`viewer` / `admin` / `super_admin`) |
| `scope_level` + `scope_values` | **אילו תלמידים** נראים (מועצתי / יישובי / בית-ספרי) |
| `is_suspended` | חוסם הכול — גם טוקן שכבר הונפק |

**הכול נאכף ב-RLS, לא בתצוגה.** מנהל-על עובר את `has_authority` לכל רשות;
משתמש רגיל מוגבל ל-`authority_codes` שלו. פרטים: [decisions/004](docs/decisions/004-admin-interface-and-agent.md).

**פעולות שמחייבות `service_role`** (יצירת משתמש, קביעת סיסמה, השהיה) רצות
ב-Edge Function `admin-users` בלבד — לעולם לא בדפדפן.

## עדכון חודשי

```bash
python scripts/update_from_moe.py --code 1400000
```
מריץ את ה-pipeline של איתי, מאתר את הקובץ הראשי, וטוען ל-Supabase.
פרטי החיבור נטענים מ-`.env.db`. ראה [docs/data-loading.md](docs/data-loading.md).

## מה בונים (שלב א')

1. מערכת הרשאות — כניסה, ניהול משתמשים, שיוך לרשות
2. טבלה ראשית — כל תלמידי הרשות עם ~20 שדות ליבה כברירת מחדל
3. תצורות שדות (Presets) — 4 ברירות מחדל: תלמידים, פרטי קשר, מוסדות, חינוך מיוחד
4. בורר שדות — הוספה/הסרה של שדות מתוך ~150 שדות זמינים
5. דפדוף שדות — כשחורגים מרוחב המסך, הטבלה מתחלקת לדפים עם חצי ניווט
6. סינון מתקדם — שווה, שונה, מכיל (substring), אינו מכיל, מתחיל ב, מסתיים ב, גדול מ, קטן מ, בין...ל, ריק, לא ריק, אחד מתוך רשימה
7. מיון — עולה/יורד בלחיצה על כותרת עמודה
8. כרטיס תלמיד — לחיצה על שורה פותחת כרטיס מפורט, ניווט הבא/הקודם
9. ייצוא לאקסל — כל השדות שנבחרו מכל הדפים

## מבנה מסד הנתונים

```
students_{code}              — טבלה ראשית לכל רשות (למשל students_14000)
users                        — טבלת משתמשים + שיוך לרשויות
```

- אינדקס ראשי: תעודת זהות
- RLS: כל משתמש רואה רק את הרשויות שמשויכות אליו
- הטבלה הראשית נדרסת בכל עדכון חודשי

## שדות ליבה (ברירת מחדל בתצורת "תלמידים")

תעודת זהות, שם משפחה, שם פרטי, מגדר, תאריך לידה, סמל מוסד, שם מוסד, רשות מוסד, רשות תלמיד, יישוב מגורים, שלב חינוך, שכבה, כיתה (משולב), סוג חינוך מוסד, סוג כיתה, סוג פיקוח, סטטוס חינוך מיוחד, סטטוס תלמיד ברשות.

שדות מסומנים "מחושב" (כיתה משולב, סטטוס חינוך מיוחד, סטטוס תלמיד ברשות) נוצרים בעיבוד ולא מגיעים ישירות ממשרד החינוך.

## מבנה תיקיות

```
docs/                        — תיעוד פרויקט (Obsidian vault)
docs/project-spec.md         — מסמך אפיון מלא (המקור האמיתי)
docs/decisions/              — החלטות שמתקבלות תוך כדי פיתוח
docs/meetings/               — סיכומי פגישות
```

## כשמתלבט

- תקרא שוב את `docs/project-spec.md`
- אם ההחלטה לא מכוסה שם, תשאל אותי
- אם עשית החלטה טכנית משמעותית, תכתוב אותה בקובץ חדש ב-`docs/decisions/`